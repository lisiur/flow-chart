class Axis {
    constructor(rect) {
        this.origin = rect.clone();
        this.bounds = rect.clone();
    }
    move(vec) {
    }
    scale(vec, dRatio) {
    }
    translate(vec) {
    }
}
export default Axis;
